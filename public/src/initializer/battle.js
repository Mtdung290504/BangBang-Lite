/**
 * @typedef {import('socket.io-client').Socket} Socket
 * @typedef {import('../network/assets_managers/assets-storage.js')} Storage
 * @typedef {ReturnType<import('../network/socket/getSandboxSocket.js')['default']>} SandboxSocket
 */

/**
 *
 */
export function initBattle(socket) {}
