export default class SkillRequirementComponent {
	/**
	 * @param {import('.types-system/dsl/skills/skill-consumption').SkillConsumption} manifest
	 */
	constructor(manifest) {
		this.manifest = manifest;
	}
}
