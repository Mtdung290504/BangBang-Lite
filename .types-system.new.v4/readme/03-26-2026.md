Backup prompt:
khoan nhé, tôi đang nghĩ đến hướng đi mới, trải phẳng hết đống skill ra thành record<string, manifest>. Loại bỏ generic thừa thãi như effects string (giảm phức tạp, chấp nhận phải copy name và warning khi không tìm thấy trong implement) và phase nữa. Tôi nghĩ cơ chế phase hiện tại sẽ đảm bảo bế tắc khi có 2 skill cần đổi loại tại thời điểm và điều kiện khác nhau. Và multi stage skill có thể tái quản lý với phase và effect để điều chỉnh phase. Vì vậy, tôi đang hình dung kiểu:

- Skill là record có name. Mỗi skill có condition trigger, ví dụ on-key:s1-key, on-key:all, các tank event cho skill bị động, on-ready khi skill đã hồi và đảm bảo consumption,...; và phase-has:..., có thể thêm/remove phase chẳng hạn, từ đó skill không bị lồng sâu bởi phase hay stage nữa mà là flatten record. Khi đó skill bị động và chủ động không còn phân biệt có hay không có multi stage nữa. Skill stack hay không stack hoàn toàn giống nhau với stack và stack đơn thuần là phần mở rộng.
- Cách trên sẽ đánh đổi bằng cách mỗi frame đều phải lặp qua tất cả skill và check condition, trace-off cho sự tối giản hóa này có đáng không?
- Tuy nhiên, có bất cập là skill đòi hỏi active lock target. Đang bất cập là skill đòi hỏi active lock target có nhiều context hơn, nhưng skill bị động thì không được active-lock, nhưng các loại targeting khác chọn tự động từ context thì có thể. Làm sao để gỡ nút thắt này?
- Nếu skill bị động kích hoạt thẳng bằng tank-events thì sẽ bị lặp 2 chỗ khác nhau nhưng logic giống nhau với on-event trong effect nhỉ? Vậy thì loại bỏ on-event?
- Đọc lại toàn bộ thư mục .types-system.new.v2 và suy xét cực kỹ càng xem có đáng thực hiện thay đổi không (tôi nghĩ bắt buộc, vì hiện tại phase đang bị giam, không cần phải chiều ý tôi, cứ nêu quan điểm khách quan từ bạn, tôi cần quan điểm khách quan của dev giàu kinh nghiệm) và lên kế hoạch trước.
