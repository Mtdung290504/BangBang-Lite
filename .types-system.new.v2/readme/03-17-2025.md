Note:

- Xong pierce và bounce:
    - .types-system.new\dsl\combat\props.type-components.ts
    - .types-system.new\dsl\physic\collider.type-conponents.ts
    - Sử dụng impact-capacity để quy định số lần va chạm
      -> bỏ limit trong bounce và có thể implement xuyên qua x mục tiêu thay vì all
    - Pierce và bounce mà cùng tồn tại thì ưu tiên pierce, vì bounce là đập vào mới nảy, mà xuyên qua thì không tính là đập.

- Vấn đề mới:
  Xem lại `apply-effect.type-entities.ts` và `apply-effect.types.ts`
  tôi nhận ra cơ chế hiện tại quá ngây thơ. Ví dụ chỉ có thể tăng tốc độ theo point hoặc %, không thể implement kiểu HP càng thấp chạy càng nhanh (tăng tốc tính từ % máu). Và nhìn lại thật ra thì nhận damage/hồi máu/sửa năng lượng và các buff stat như tốc chạy tốc đánh suy cho cùng đều là điều chỉnh stat nên tôi nghĩ:
    - Tất cả stat đều nên được phép tính như dealt-damage, tức có thể lấy cả từ stat khác của cả mình cả địch (VD cướp tốc độ)
    - Cần cơ chế tổng quát mọi cái điều chỉnh chỉ số vì giống nhau, nhưng đảm bảo gây damage/hồi HP/điều chỉnh năng lượng chỉ kích hoạt 1 lần thay vì theo duration và gây damage/hồi máu có nên bỏ khai báo text visual và để engine tự quyết định màu theo value âm hay dương và tự rẽ góc khi đang có nhiều luồng damage không?
    - Nhận ra việc tăng hay giảm damage/hồi máu/stat khác đều có cơ chế tăng giảm khác nhau, ví dụ damage có thể giảm theo giáp hoặc không nếu là damage chuẩn, có buff giảm damage skill hay đánh thường hay miễn thương tổng thể; hồi HP thì tăng giảm theo hiệu ứng hồi máu (có thể có buff tăng hoặc giảm); điều chỉnh tốc độ có thể tăng hay giảm theo % hiệu ứng gia tốc;...
    - Vậy có nên mở rộng chỉ số và DSL cho phép tận dụng thêm function để tính ra các giá trị và đơn vị % hay unit không? Chứ hiện tại string expression không đủ đáp ứng và khá phức tạp/dễ quên, còn function có khả năng tùy biến cao hơn, cung cấp full đầu vào, return đúng 1 giá trị.
